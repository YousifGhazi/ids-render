{
  "name": "mock-api",
  "version": "1.0.0",
  "description": "Mock API server for Users CRUD operations",
  "main": "server.js",
  "scripts": {
    "start": "json-server --watch db.json --port 3002",
    "dev": "json-server --watch db.json --port 3002 --delay 500",
    "reset": "node reset-db.js"
  },
  "dependencies": {
    "json-server": "^0.17.4"
  },
  "devDependencies": {}
}
